import { Button } from 'antd';
import { styled } from 'styled-components';

const AttendancePage = () => {
  return (
    <AttendancePageStyled>
      <h1>Trang nghĩ phép</h1>
    </AttendancePageStyled>
  );
};

export default AttendancePage;
const AttendancePageStyled = styled.div`

`;